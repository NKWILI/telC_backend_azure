export const ROOM_GATEWAY_NAMESPACE = '/speaking-room';

/**
 * A room's short code: 6 characters read aloud between two students in the
 * same classroom (D32). 30^6 ≈ 729 million values, unique among live rooms.
 */
export const ROOM_CODE_LENGTH = 6;
